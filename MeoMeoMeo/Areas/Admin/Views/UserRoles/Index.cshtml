@model IEnumerable<MeoMeoMeo.Models.UserRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserRole1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserRole1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaUR }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaUR }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaUR })
        </td>
    </tr>
}

</table>
